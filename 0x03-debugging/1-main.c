#include <stdio.h>

/**
 * main - returns the largest of 3 numbers
 * @void: void
 * description: Comment out the part causing the loop
 * Return: 0
 */

int main(void)
{
int i;
printf("Infinite loop incoming :(\n");
/*
 * i = 0;
 * while (i < 10)
 * {
 *	putchar(i);
 * }
 */
printf("Infinite loop avoided! \\o/\n");
return (0);
}
