#include "holberton.h"
/**
 *  add - adds two integers and returns the result.
 *
 * @x: int type
 * @y: int type
 * Return: Returns the value of n the variable in this case.
 */
int add(int x, int y)
{
int n;
n = x + y;
return (n);
}
