#include "holberton.h"

/**
 * mul - function that multiplies two integers.
 * @a: int type number
 * @b: int type numbr
 * Return: 1 if uppercase, else 0Returns multiplic
 */
int mul(int a, int b)
{
int  Mult;
Mult = a * b;
return (Mult);
}
